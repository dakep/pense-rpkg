language: r
cache: packages
r:
  - oldrel
  - release
  - devel
warnings_are_errors: true

env:
  global:
    - MAKEFLAGS="-j 2"

branches:
  only:
   - master
   - develop
   - /^release\/v?[\d\.]+$/
