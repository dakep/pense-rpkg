language: r
cache: packages
r:
  - oldrel
  - release
  - devel
warnings_are_errors: false

env:
  global:
    - MAKEFLAGS="-j 2"

branches:
  only:
   - master
   - develop
   - /^release\/v[\d\.]+$/
