# Ridge optimizer using an Augmented data matrix. Only available for Ridge problems (\`alpha=0“) and selected automatically in this case.

Ridge optimizer using an Augmented data matrix. Only available for Ridge
problems (\`alpha=0“) and selected automatically in this case.

## Usage

``` r
en_ridge_options()
```
